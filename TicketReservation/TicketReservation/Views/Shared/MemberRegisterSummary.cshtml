@model TicketReservation.Models.AuthModelView

<div class="col-md-6">
    <h4 class="accountText">SZYBKA REJESTRACJA</h4>

    @Html.ValidationSummary(true)

    @using (Html.BeginForm("MemberRegisterSummary", "Auth"))
    {
        @Html.TextBoxFor(m => m.RegisterModel.Email, null, new { @class = "form-control", placeholder = "Adres email" })
        <br />
            @Html.TextBoxFor(m => m.RegisterModel.Password, null, new { @class = "form-control", placeholder = "Hasło" })
            <br />
            @Html.TextBoxFor(m => m.RegisterModel.RepeatPassword, null, new { @class = "form-control", placeholder = "Powtórz hasło" })
            <br />
            <button type="submit" class="btn btn-primary btn-block">Zarejestruj</button>
    }
</div>

